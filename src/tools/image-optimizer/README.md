# Image Optimizer

Uses browser image decoding and Canvas to resize, crop, compress, batch-export, and compose Open Graph images. JPEG, PNG, WebP, and AVIF output is offered when supported.

Animated inputs are flattened. Canvas re-encoding may discard metadata and color profiles or alter colors, and browser codec support varies. Inputs and output pixel counts are bounded to protect browser memory.
