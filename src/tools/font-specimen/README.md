# Font Specimen

Loads local browser-readable font files for specimen previews, scale exploration, and CSS/design-token export. Font bytes remain in the page.

Font parsing, variable axes, hinting, and rendering differ across browsers and operating systems. Generated scales and tokens are starting points, and loading a file does not grant a webfont license.
