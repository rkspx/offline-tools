# DevTools Studio

Provides local structured-text formatting/minification, JSONPath-like expressions, TypeScript shape generation, log parsing, text comparison, and reusable snippets stored in browser storage.

The transformers are intentionally lightweight rather than standards-complete parsers or compilers. Generated types and expression results require review, and clearing site data removes saved snippets.
