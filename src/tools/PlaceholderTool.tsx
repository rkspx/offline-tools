import { EmptyState } from "../components/FoundationStates";

export default function PlaceholderTool() {
  return (
    <EmptyState
      title="Implementation pending"
      description="The workspace is ready. Processing features will be added in a later phase."
    />
  );
}
