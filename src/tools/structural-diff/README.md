# Structural Diff

Parses JSON, YAML, or XML into comparable structures, reports path-level changes, supports merge choices, and exports the resulting document.

Normalization focuses on data rather than source presentation. Comments, YAML anchors/styles, XML formatting, attribute-order intent, and other syntax-level details may be lost or rearranged. Semantic meaning outside the parsed structure is not inferred.
