# Palette and Contrast

Samples a local image through Canvas to extract representative colors, compares foreground/background pairs using WCAG contrast math, previews text, and exports palette tokens.

Extraction is an approximation influenced by sampling and browser color handling. Pairwise contrast does not cover focus, hover, transparency, gradients, typography, or complete interface accessibility; test real UI states separately.
